<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <form action="/products/<%=prod._id%>?_method=PUT" method="POST">
        <label for="name">Name of the products</label>
        <input type="text" id="name" name="name" placeholder="product name" value="<%=prod.name%>">
        <label for="price">Price of the products</label>
        <input type="number" id="price" name="price" placeholder="product price" value="<%=prod.price%>">
        <label for="category"></label>
        <select name="category" id="category" >
            <option value="fruit" <%=prod.category === 'fruit' ? 'selected' :''%>>fruit</option>
            <option value="vegetable" <%=prod.category === 'vegetable' ? 'selected' :''%>>vegetable</option>
            <option value="dairy" <%=prod.category === 'dairy' ? 'selected' :''%>>dairy</option>
        </select>
        <button>Submit</button>
    </form>
</body>
</html>